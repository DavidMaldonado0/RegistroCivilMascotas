import React from 'react';
import PropTypes from 'prop-types';

const ProfileItem = (props) => {
  return <div>Perfil de la mascota</div>;
};

ProfileItem.propTypes = {};

export default ProfileItem;
